enclave {

    trusted {
        /* define ECALLs here. */
		public int uc_generateToken();
		public int uc_check_proof([user_check]int* proof);
		public void tre_bill_calculation(int random_token, [user_check]int* proof);

    };

    untrusted {
        /* define OCALLs here. */
		
    };
};
